{{ form_start(form) }}
    {{ form_row(form.nomAg) }}
    {{ form_row(form.adresseAg) }}
    <div class="row">
        <div class="col-md-6">{{ form_row(form.telephone) }}</div>
        <div class="col-md-6">{{ form_row(form.email) }}</div>
    </div>
    {{ form_row(form.horaires) }}

    <button class="btn btn-primary">Enregistrer</button>
    <a href="{{ path('admin_banques_index', {banque: banque.id}) }}" class="btn btn-secondary">Retour</a>
{{ form_end(form) }}