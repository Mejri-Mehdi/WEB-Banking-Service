{{ form_start(form) }}
    {{ form_row(form.nomBq) }}
    <div class="row">
        <div class="col-md-6">{{ form_row(form.emailBq) }}</div>
        <div class="col-md-6">{{ form_row(form.telephoneBq) }}</div>
    </div>
    {{ form_row(form.siteWeb) }}
    {{ form_row(form.statut) }}
    {{ form_row(form.description) }}

    <button class="btn btn-primary">Enregistrer</button>
    <a href="{{ path('admin_banques_index') }}" class="btn btn-secondary">Retour</a>
{{ form_end(form) }}