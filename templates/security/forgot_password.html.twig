<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - Beeline Banking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #0f2849 0%, #1a4d8f 50%, #1e3a5f 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Poppins', sans-serif;
        }
        .card {
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            width: 100%;
            max-width: 500px;
            border: none;
        }
        .card-body {
            padding: 3rem;
        }
        .btn-primary {
            background: #3b82f6;
            border: none;
            padding: 12px;
            font-weight: 600;
        }
        .btn-primary:hover {
            background: #2563eb;
        }
        .form-control {
            padding: 12px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="card-body text-center">
            <div class="mb-4">
                <i class="bi bi-key-fill text-primary" style="font-size: 3rem;"></i>
            </div>
            <h2 class="mb-3 fw-bold">Mot de passe oublié ?</h2>
            
            {% for message in app.flashes('success') %}
                <div class="alert alert-success mt-3 text-start">{{ message|raw }}</div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="alert alert-danger mt-3 text-start">{{ message }}</div>
            {% endfor %}
            
            <p class="text-muted mb-4">Entrez votre adresse email et nous vous enverrons les instructions pour réinitialiser votre mot de passe.</p>

            <form method="post">
                <div class="mb-3 text-start">
                    <label class="form-label">Email</label>
                    <input type="email" name="email" class="form-control" placeholder="votre.email@exemple.com" required>
                </div>
                <button type="submit" class="btn btn-primary w-100 mb-3">Envoyer le lien</button>
            </form>

            <a href="{{ path('app_login') }}" class="text-decoration-none text-muted">
                <i class="bi bi-arrow-left me-1"></i>Retour à la connexion
            </a>
        </div>
    </div>
</body>
</html>
