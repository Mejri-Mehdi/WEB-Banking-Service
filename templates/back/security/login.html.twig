<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beeline - Connexion</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
            min-height: 100vh;
            color: #333;
        }

        /* Header avec logo */
        .logo-header {
            background: white;
            padding: 25px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
        }

        .logo-container {
            display: flex;
            align-items: center;
            gap: 25px;
        }

 .logo-circle {
    width: 70px;
    height: 70px;
    border-radius: 50%;
    background: white;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    border: 4px solid #e6f0ff;
}

.logo-circle img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Changed from 'contain' to 'cover' */
    object-position: center center; /* Ensures the center is focused */
    padding: 0;
}
        .app-title {
            font-size: 2.8rem;
            font-weight: 800;
            color: #0052cc;
            margin: 0;
        }

        .back-link {
            color: #666;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #0052cc;
        }

        /* Formulaire de connexion */
        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: calc(100vh - 150px);
            padding: 40px 20px;
        }

        .login-card {
            background: white;
            border-radius: 30px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            padding: 60px 50px;
            text-align: center;
            border: 2px solid #e6f0ff;
        }

        .login-title {
            font-size: 2.5rem;
            color: #0052cc;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .login-subtitle {
            color: #666;
            font-size: 1.2rem;
            margin-bottom: 40px;
        }

        .form-group {
            margin-bottom: 30px;
            text-align: left;
        }

        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            color: #555;
            font-size: 1.1rem;
        }

        .input-with-icon {
            position: relative;
        }

        .input-with-icon input {
            width: 100%;
            padding: 18px 20px 18px 55px;
            border: 2px solid #e0e0e0;
            border-radius: 15px;
            font-size: 1.1rem;
            transition: all 0.3s;
            background: #f9f9f9;
        }

        .input-with-icon input:focus {
            outline: none;
            border-color: #0052cc;
            background: white;
            box-shadow: 0 0 0 3px rgba(0, 82, 204, 0.1);
        }

        .input-icon {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: #777;
            font-size: 1.2rem;
        }

        .forgot-password {
            text-align: right;
            margin-top: 10px;
        }

        .forgot-password a {
            color: #0052cc;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }

        .btn-login {
            width: 100%;
            padding: 20px;
            background: #0052cc;
            color: white;
            border: none;
            border-radius: 15px;
            font-size: 1.3rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 20px;
        }

        .btn-login:hover {
            background: #0041a3;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 82, 204, 0.2);
        }

        .separator {
            display: flex;
            align-items: center;
            margin: 40px 0;
            color: #999;
        }

        .separator::before, .separator::after {
            content: '';
            flex: 1;
            height: 1px;
            background: #e0e0e0;
        }

        .separator span {
            padding: 0 20px;
            font-weight: 600;
        }

        .social-login {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 40px;
        }

        .social-btn {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            text-decoration: none;
            transition: all 0.3s;
        }

        .social-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .btn-google {
            background: #DB4437;
        }

        .btn-facebook {
            background: #4267B2;
        }

        .btn-apple {
            background: #000;
        }

        .register-link {
            margin-top: 30px;
            color: #666;
            font-size: 1.1rem;
        }

        .register-link a {
            color: #0052cc;
            text-decoration: none;
            font-weight: 700;
        }

        .register-link a:hover {
            text-decoration: underline;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 30px 20px;
            color: #888;
            border-top: 1px solid #e0e0e0;
            background: white;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .logo-header {
                flex-direction: column;
                gap: 20px;
                padding: 20px;
                text-align: center;
            }
            
            .logo-container {
                flex-direction: column;
                gap: 15px;
            }
            
            .login-card {
                padding: 40px 30px;
                margin: 20px;
            }
            
            .login-title {
                font-size: 2.2rem;
            }
            
            .social-login {
                gap: 15px;
            }
            
            .social-btn {
                width: 50px;
                height: 50px;
                font-size: 1.3rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header avec logo -->
    <header class="logo-header">
        <div class="logo-container">
            <div class="logo-circle">
                <div style="width: 100%; height: 100%; background: #0052cc; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-size: 2rem; font-weight: bold;">
                    B
                </div>
            </div>
            <h1 class="app-title">Beeline</h1>
        </div>
        
        <div>
            <a href="{{ path('app_security') }}" class="back-link" style="margin-right: 20px;">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            <a href="{{ path('app_home') }}" class="back-link">
                <i class="fas fa-home"></i> Accueil
            </a>
        </div>
    </header>

    <!-- Formulaire de connexion -->
    <main class="login-container">
        <div class="login-card">
            <h2 class="login-title">Se connecter</h2>
            <p class="login-subtitle">Accédez à vos services bancaires Beeline</p>
            
            <form id="loginForm" action="{{ path('app_login') }}" method="POST">
                <div class="form-group">
                    <label for="email">Adresse email</label>
                    <div class="input-with-icon">
                        <i class="fas fa-envelope input-icon"></i>
                        <input type="email" id="email" name="email" placeholder="votre@email.com" required>
                    </div>
                </div>
                
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <div class="input-with-icon">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" id="password" name="password" placeholder="Votre mot de passe" required>
                    </div>
                    <div class="forgot-password">
                        <a href="#">Mot de passe oublié ?</a>
                    </div>
                </div>
                
                <button type="submit" class="btn-login">
                    <i class="fas fa-sign-in-alt"></i> Se connecter
                </button>
            </form>
            
            <div class="separator">
                <span>Ou connectez-vous avec</span>
            </div>
            
            <div class="social-login">
                <a href="#" class="social-btn btn-google">
                    <i class="fab fa-google"></i>
                </a>
                <a href="#" class="social-btn btn-facebook">
                    <i class="fab fa-facebook-f"></i>
                </a>
                <a href="#" class="social-btn btn-apple">
                    <i class="fab fa-apple"></i>
                </a>
            </div>
            
            <div class="register-link">
                Vous n'avez pas encore de compte ? <a href="{{ path('app_register') }}">Créer un compte</a>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2023 Beeline - Services Bancaires Clients. Tous droits réservés.</p>
        <p style="margin-top: 10px; font-size: 0.9rem;">Une connexion sécurisée à vos services bancaires</p>
    </footer>

    
</body>
</html>